import Button from '../button';
import Screen from './screen';

class AppraisalScreen extends Screen {
    constructor(public saveButton: Button
                ) {
        super();
    }
}

export default AppraisalScreen;
